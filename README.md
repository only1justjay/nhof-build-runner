# nhof-build-runner
Public CI runner (free minutes). Holds NO source — checks out the private NHOF
plugin repos with a token at build time, builds Windows retail, uploads artifacts.
Temporary; delete after use.
